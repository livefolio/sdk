# Portfolio Module

Brokerage account aggregation and trade order management.

> This module is under active development. The interface is currently empty and will be populated as features are implemented.

## Setup

```ts
const portfolio = livefolio.portfolio;
```

## Current Status

```ts
interface PortfolioModule {
  // Methods will be added as portfolio features are implemented
}
```

`createPortfolio(client)` currently returns an empty object.
