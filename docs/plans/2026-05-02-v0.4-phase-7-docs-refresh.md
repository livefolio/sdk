# v0.4 Phase 7 — Documentation refresh

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers-extended-cc:subagent-driven-development` (recommended) or `superpowers-extended-cc:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Companion spec:** `docs/specs/2026-05-02-v0.4-phase-7-docs-refresh-design.md`

**Depends on:** Phase 6 must be merged first. This plan describes only documentation changes; the source-tree move it relies on is phase 6.

**Goal:** Bring all user-facing and AI-agent-facing docs into alignment with the v0.4 public surface. After this phase, no doc references `createClient`, `LivefolioClient`, `Supabase`, `FRED`, or v0.3 handle classes (except inside `parity/src/v3/`'s own AGENTS.md files, which describe v0.3 internals correctly).

**Tech stack:** Markdown only. No source changes.

---

## Tasks

### Task 1 — Author runnable v0.4 quick-start example

**Goal:** Write a TypeScript file that compiles and runs against the v0.4 surface, demonstrating the SPY/QQQ/IEF canonical pair end-to-end. This file is the source of truth for the README quick-start; we verify-by-running before pasting into the README.

**Files:**
- Create: `scripts/v04-readme-example.ts` — full runnable strategy with `tactical.fromSpec`, `runBacktest`, `YfinanceDataFeed`, `USEquityCalendar`, `MemoryFeatureCache`. Print final snapshot.

**Verify:**
- `npx tsx scripts/v04-readme-example.ts` runs to completion, prints a non-empty final snapshot.
- `npx tsc --noEmit scripts/v04-readme-example.ts` clean.

**Acceptance:**
- [ ] Script runs end-to-end without error.
- [ ] Output includes a final-date portfolio snapshot with non-zero positions.
- [ ] Script imports only from `@livefolio/sdk`, `@livefolio/sdk/tactical`, `@livefolio/datafeed-yfinance`. No internal subpaths.

---

### Task 2 — Rewrite `README.md`

**Goal:** Replace the v0.3-flavored README with a 100-line v0.4 orientation. Spec § "Content design → README.md" gives the structure.

**Files:**
- `README.md` (full rewrite)

**Quick-start in the README:** copy the body of `scripts/v04-readme-example.ts` (without the IIFE wrapping). Both must stay in sync; future drift is caught by re-running task 1's script.

**Verify:**
- `grep -E "createClient|LivefolioClient|TickerHandle|IndicatorHandle|SignalHandle|AllocationHandle|StrategyHandle|PortfolioHandle|SimulationHandle|Supabase|FRED|supabase-js" README.md` returns nothing.
- The quick-start code block in `README.md` matches `scripts/v04-readme-example.ts` body verbatim (use copy-paste, then verify by extracting and re-running).

**Acceptance:**
- [ ] README has a single quick-start example, runnable verbatim.
- [ ] Sections present: Install, Quick start, Concepts (TacticalSpec, DataFeed, Calendar, FeatureCache, Executor), API reference (subpath import map), Design (pointers to specs), License.
- [ ] No v0.3 terms anywhere in the file.

---

### Task 3 — Rewrite root `AGENTS.md`

**Goal:** Bring the root AGENTS.md in line with the v0.4 + parity-as-regression-target reality. Spec § "Content design → Root `AGENTS.md`" gives the structural changes.

**Files:**
- `AGENTS.md` (root)

**Specific edits:**
- Drop "backed by a Supabase database" from the Purpose section.
- Add `parity/` row to Subdirectories table.
- Replace "Fluent builder API" pattern with "Spec-driven strategies" and "Pluggable runtime layers" patterns.
- Verify `nanoid` is still a dependency; if not, remove from External Dependencies.

**Verify:**
- `grep -i "supabase\|fluent builder\|TickerHandle" AGENTS.md` returns nothing.
- `grep "parity/" AGENTS.md` finds the new subdirectory row.

**Acceptance:**
- [ ] No v0.3 terminology.
- [ ] Subdirectories table includes `src/`, `docs/`, `parity/`.
- [ ] External Dependencies section accurately reflects current `package.json` deps.

---

### Task 4 — Rewrite `src/AGENTS.md`

**Goal:** Replace the v0.3-tree description with the v0.4 module map. Spec § "Content design → `src/AGENTS.md`" gives the new contents.

**Files:**
- `src/AGENTS.md` (full rewrite)

**Specific edits:**
- Subdirectories table replaced with: `interfaces/`, `strategy/`, `features/`, `tactical/`, `reference/`, `orders/`, `portfolio/`.
- Architecture Flow replaced with v0.4 flow (`tactical.fromSpec` → `Strategy` → `runBacktest` → `BacktestResult`).
- Common Patterns: drop "Lazy resolution" and "Upsert-on-resolve"; add "Content-addressed feature cache", "Pluggable runtime layers".

**Verify:**
- `grep -E "handles/|computations/|providers/|backtest/|metrics/" src/AGENTS.md` returns nothing.
- All seven v0.4 subdirectories listed.

**Acceptance:**
- [ ] Subdirectories table matches the seven v0.4 directories under `src/`.
- [ ] Architecture flow uses v0.4 names.
- [ ] No v0.3 patterns mentioned.

---

### Task 5 — Author per-module `AGENTS.md` files

**Goal:** Add an `AGENTS.md` to each v0.4 module that lacks one. Spec § "Per-module `AGENTS.md` — template" defines the template.

**Files (create):**
- `src/interfaces/AGENTS.md`
- `src/strategy/AGENTS.md`
- `src/features/AGENTS.md`
- `src/tactical/AGENTS.md`
- `src/reference/AGENTS.md`
- `src/orders/AGENTS.md`
- `src/portfolio/AGENTS.md`

Each file is ~30-60 lines. Each follows the existing AGENTS.md tone: terse, agent-as-reader, with Purpose / Key Files table / For AI Agents / Common Patterns sections.

**Verify:**
- `find src -maxdepth 2 -name AGENTS.md | sort` lists eight files: `src/AGENTS.md` plus seven module-level files.
- Each file references real symbols/files in its directory (no copy-paste errors).

**Acceptance:**
- [ ] Seven new AGENTS.md files exist.
- [ ] Each describes only the contents of its own directory.
- [ ] Each is terse — no marketing copy, no v0.3 references.

---

### Task 6 — Update `CLAUDE.md`

**Goal:** Surgical edits to remove the stale Supabase paragraph and update the "Do Not" section to v0.4-relevant guidance. Spec § "Content design → `CLAUDE.md`" gives the new "Do Not" content.

**Files:**
- `CLAUDE.md`

**Specific edits:**
- Remove the three "Do Not" bullets that reference `database.types.ts`, Supabase migrations, and "mock Supabase".
- Add the three new bullets about `src/index.ts`, mocking `DataFeed`/`Executor`, and `parity/src/v3/` discipline.
- Verify the "Project Conventions" and "Key Commands" sections still apply (they should — those are language/tool conventions, not API-specific).

**Verify:**
- `grep -i "supabase\|database\.types" CLAUDE.md` returns nothing.
- `grep "parity/src/v3" CLAUDE.md` finds the new "Do Not" bullet.

**Acceptance:**
- [ ] No Supabase references.
- [ ] "Do Not" section has three v0.4-relevant items.
- [ ] Other sections unchanged.

---

### Task 7 — Light edit `docs/AGENTS.md`

**Goal:** Verify `docs/AGENTS.md` accurately describes the contents of `docs/` after phases 6 and 7 land. Most likely a one-line tweak or no change.

**Files:**
- `docs/AGENTS.md` (read; edit only if needed)

**Verify:**
- The file's description of `specs/` and `plans/` still accurately reflects what's in those directories.

**Acceptance:**
- [ ] File reviewed; either confirmed accurate or updated.

---

### Task 8 — Update `yfinance/README.md` (sibling-repo commit)

**Goal:** Update the yfinance README to reference v0.4 explicitly and link to the new SDK README's example. Separate commit in `~/Documents/Personal/livefolio-2/yfinance/`.

**Files (in yfinance repo, not this one):**
- `yfinance/README.md`

**Specific edits:**
- Confirm package description states "v0.4 SDK adapter."
- Update any code example that uses v0.3 surface (likely none, since yfinance only ever consumed v0.4 types).
- Link to `@livefolio/sdk` README's quick-start as the canonical end-to-end example.

**Verify:**
- `grep -E "createClient|TickerHandle|Supabase" yfinance/README.md` returns nothing.

**Acceptance:**
- [ ] yfinance README references v0.4.
- [ ] No v0.3 terminology.
- [ ] Commit references this phase 7 plan.

---

### Task 9 — Final doc-grep sweep

**Goal:** A single grep that confirms no v0.3 terminology lingers in any user-facing or AI-agent-facing doc.

**Files:** none modified — verification only.

**Verify command:**
```
grep -rEi "supabase|createClient|LivefolioClient|TickerHandle|IndicatorHandle|SignalHandle|AllocationHandle|StrategyHandle|PortfolioHandle|SimulationHandle|FRED" \
  README.md AGENTS.md CLAUDE.md \
  src/AGENTS.md src/*/AGENTS.md \
  docs/AGENTS.md
```

Expected output: empty (no matches).

**Acceptance:**
- [ ] grep returns nothing.
- [ ] `npm test` and `cd parity && npm test` still green (no source changes, but verify the docs commit didn't break a path-referenced test or example).

---

## Out of scope

- **Phase 8 (user wiki).** Hosted docs site, comprehensive guides, recipes — separate brainstorm.
- **API reference generation** (typedoc, etc.) — phase 8 territory.
- **`docs/specs/*.md`, `docs/plans/*.md`** — historical record, not rewritten.
- **`parity/src/v3/**/AGENTS.md`** — describes v0.3 internals correctly from inside the v0.3 source tree; no edits.
- **Internal yfinance docs** (its own AGENTS.md tree) — that repo's concern.

## Sequencing

Tasks 1-7 can be done in any order in this repo (they don't conflict). Task 8 happens in the yfinance repo and lands the same day as phase 6's version bump (per phase 6's plan). Task 9 is the final gate and runs last.
